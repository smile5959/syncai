# 다음 세션 작업 프롬프트

아래 내용을 그대로 Claude에게 붙여넣기 하세요.

---

## 프롬프트

SyncAI 프로젝트 이어서 작업.
시작 전 메모리에서 MEMORY.md → todo_syncai.md → project_syncai.md 순서로 읽을 것.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[ 작업 ] SyncAI MCP 클라이언트 인스톨러 제작
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 배경

SyncAI는 AI가 팀원(Worker) 로컬 PC 파일에 접근하는 협업툴.
MCP 서버가 Worker PC에서 실행되고 Cloudflare Tunnel로 백엔드와 연결됨.
클로드 데스크탑처럼 .exe 인스톨러로 배포해서 설치 마찰을 없애는 게 목표.

## 인스톨러 흐름 (확정)

```
사용자가 .exe 실행
→ [1] 이용 동의 화면
→ [2] SyncAI 로그인 (이메일 + 비밀번호)
       → 백엔드 /auth/login 호출 → JWT 발급
       → /mcp-configs/auto-register 호출 → MCP 토큰 발급
       → .env에 자동 저장 (MCP_AUTH_TOKEN, SYNCAI_BACKEND_URL)
→ [3] AI가 접근할 폴더 선택 (base_dir)
       → .env에 자동 저장 (또는 heartbeat으로 백엔드에 전송)
→ [4] 설치 경로 선택
→ [5] 설치 실행
       → server.exe, cloudflared.exe, WinSW, syncai-mcp.xml 복사
       → WinSW로 Windows Service 등록 + 시작
→ 설치 완료 → 서비스가 자동으로 heartbeat → 백엔드 연결
```

## 파일 구조 (mcp-server 폴더 기준)

```
syncai-backend/mcp-server/
├── server.py          ← 메인 서버 (이미 완성)
├── heartbeat.py       ← heartbeat 루프 (이미 완성)
├── config.py          ← 설정 관리 (이미 완성)
├── tools.py           ← 파일 도구 (이미 완성)
├── tunnel.py          ← Cloudflare Tunnel (이미 완성)
├── requirements.txt   ← 의존성
├── server.spec        ← [신규] PyInstaller 스펙
├── syncai-mcp.xml     ← [신규] WinSW 서비스 설정
├── setup.iss          ← [신규] Inno Setup 스크립트
└── .env.example       ← 템플릿 (이미 있음)
```

## 작업 순서

### 1. server.spec (PyInstaller)
- server.py + 모든 의존성 → server.exe 단일 파일 빌드
- cloudflared.exe는 별도 포함 (datas에 추가)
- .env는 번들 제외 (설치 시 생성)
- 숨겨진 import 처리: uvicorn, fastapi, httpx, dotenv 등
- 빌드 커맨드: `pyinstaller server.spec`

### 2. syncai-mcp.xml (WinSW)
- 서비스명: SyncAI MCP Server
- 실행파일: server.exe
- 작업 디렉토리: 설치 경로
- 크래시 재시작: 5초 후 자동
- 로그: logs\service.log (rotate, 10MB)

### 3. setup.iss (Inno Setup)
커스텀 페이지 2개 필요:
  - 로그인 페이지: 이메일/비밀번호 입력 → 백엔드 로그인 → 토큰 발급 → .env 저장
  - 폴더 선택 페이지: BrowseForFolder 다이얼로그 → base_dir → .env 저장

설치 항목:
  - server.exe (PyInstaller 빌드)
  - cloudflared.exe
  - WinSW 실행파일 (winsw.exe → syncai-mcp.exe로 복사)
  - syncai-mcp.xml
  - .env (로그인 + 폴더 선택 결과로 생성)

설치 후 자동 실행:
  - syncai-mcp.exe install (서비스 등록)
  - syncai-mcp.exe start (서비스 시작)

언인스톨러:
  - syncai-mcp.exe stop
  - syncai-mcp.exe uninstall
  - 파일 삭제

## 핵심 기술 결정

- Inno Setup 로그인 페이지: Pascal Script로 WinHTTP 사용해서 백엔드 API 호출
- 토큰은 .env에 평문 저장 (서비스가 읽어서 사용)
- base_dir은 .env에 저장하거나 첫 heartbeat에 포함해서 백엔드로 전송
- cloudflared.exe: mcp-server 폴더에 이미 있음, 번들에 포함

## 관련 파일 경로

- MCP 서버: C:\khh\syncAI\syncai-backend\mcp-server\
- 백엔드 API: https://syncai-backend.fly.dev
- 로그인 엔드포인트: POST /v1/auth/login {email, password} → {token}
- auto-register: POST /v1/mcp-configs/auto-register (Bearer JWT) → {ok, mcp_token}
- heartbeat: POST /v1/mcp-configs/heartbeat {mcp_token, endpoint}

## 주의사항

- Inno Setup은 Pascal Script로 HTTP 호출 가능 (WinHTTP.WinHTTPRequest.5.1)
- PyInstaller 빌드는 Windows에서 해야 함 (cross-compile 안 됨)
- WinSW 최신 버전: https://github.com/winsw/winsw (v3.x)
- 코드 서명 인증서는 아직 없음 → 테스트 시 SmartScreen 경고 무시
- server.exe 실행 시 작업 디렉토리 = 설치 폴더여야 .env를 찾을 수 있음
