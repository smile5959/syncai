# SyncAI 다음 세션 프롬프트

> 이 파일을 복사해서 새 세션 첫 메시지로 붙여넣으면 됩니다.

---

SyncAI 프로젝트 이어서 작업. 시작 전 메모리에서 MEMORY.md → todo_syncai.md → project_syncai.md 순서로 읽을 것.

## 현재 상황 요약

### 완료된 것
- MCP 서버 보안 수정 (서버 재시작으로 적용 완료)
- 인스톨러 파일 3개 생성 및 빌드 완료:
  - `mcp-server/server.spec` (PyInstaller)
  - `mcp-server/syncai-mcp.xml` (WinSW 서비스)
  - `mcp-server/setup.iss` (Inno Setup)
  - 빌드 결과: `Output/SyncAI-MCP-Setup.exe` (41MB)
- 백엔드 rate limiting (`fly deploy` 완료)

### 문제가 된 것
인스톨러 테스트 중 MCP 등록 실패 발생. 근본 원인:
- 기존 `setup.iss`는 로그인 → `auto-register` API 호출 흐름이었음
- 그런데 `auto_register_mcp()`는 프론트에서 사전에 만들어둔 **빈 슬롯**이 있어야 동작함
- 설치 시점에 슬롯이 없으니 항상 실패 → 닭-달걀 문제

### 확정된 방향 (방향 1)
Claude Desktop처럼 **설치와 로그인을 분리**:

```
인스톨러:  이메일 + 폴더만 입력 → .env 저장 → 서비스 시작 → 브라우저 오픈
웹앱:      "내 PC 연결하기" 버튼 클릭 → 빈 MCP 슬롯 생성
서비스:    _email_bootstrap() 폴링 → 슬롯 발견 → 토큰 저장 → heartbeat → 연결 완료
```

## 지금 해야 할 작업

### 작업 1: `setup.iss` 수정 (가장 중요)

파일: `C:\khh\syncAI\syncai-backend\mcp-server\setup.iss`

**제거**:
- `DoLogin()`, `DoAutoRegister()`, `GenerateToken()`, `ExtractJSONString()` 함수 전체
- `SavedJWT`, `SavedMCPToken` 변수
- WinHTTP COM 오브젝트 관련 코드 전부
- 비밀번호 입력 필드

**변경**:
- 이메일 입력 페이지 유지 (비밀번호 제거)
- `.env` 생성 내용: `SYNCAI_EMAIL=` + `MCP_BASE_DIR=` 만 저장
- 설치 완료 시 브라우저 오픈 추가:
  ```pascal
  ShellExec('open', 'https://syncai-frontend-seven.vercel.app/rooms', '', '', SW_SHOWNORMAL, ewNoWait, ErrCode);
  ```
- Pascal 주석 안에 `{...}` 중괄호 절대 사용 금지 → `(...)` 으로 대체

### 작업 2: 프론트엔드 — "내 PC 연결하기" 버튼

파일: `C:\khh\syncAI\syncai-frontend\src\components\worker\mcp-settings-modal.tsx`

MCP 없는 empty state에 "내 PC 연결하기" 버튼 추가:
- 클릭 시 `POST /v1/mcp-configs/` with `{ name: "내 PC", base_dir: "" }` — 빈 슬롯 생성
- 성공 후: "대기 중... 서비스가 실행 중이면 자동으로 연결됩니다" 메시지
- WebSocket 이벤트(`mcp:connected`)로 연결 완료 시 상태 업데이트

### 작업 3: 백엔드 — 빈 슬롯 생성 가능 여부 확인

파일: `C:\khh\syncAI\syncai-backend\app\routers\mcp_configs.py`

`create_mcp_config()` 가 `base_dir=""` 허용하는지 확인. 필요 시 validator 완화.
`lookup-by-email` 엔드포인트가 `endpoint=""` 인 미연결 config를 올바르게 반환하는지 확인.

### 작업 4: 재빌드

```bash
cd C:\khh\syncAI\syncai-backend\mcp-server
pyinstaller server.spec
iscc setup.iss
```
`Output/SyncAI-MCP-Setup.exe` 생성 확인.

## 키 파일 위치

| 파일 | 경로 |
|------|------|
| 인스톨러 스크립트 | `mcp-server/setup.iss` |
| PyInstaller 스펙 | `mcp-server/server.spec` |
| WinSW 서비스 설정 | `mcp-server/syncai-mcp.xml` |
| MCP 서버 메인 | `mcp-server/server.py` |
| heartbeat / bootstrap | `mcp-server/heartbeat.py` |
| 백엔드 MCP 라우터 | `syncai-backend/app/routers/mcp_configs.py` |
| 프론트 MCP 모달 | `syncai-frontend/src/components/worker/mcp-settings-modal.tsx` |

## 배포 현황

- 백엔드: `https://syncai-backend.fly.dev` (Fly.io)
- 프론트: `https://syncai-frontend-seven.vercel.app` (Vercel)
- MCP 서버: Worker PC 로컬 실행 (Cloudflare Tunnel로 공개)
